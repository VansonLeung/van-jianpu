{
  "name": "van-jianpu-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Jianpu image transcription, notation editing and Erhu playback",
  "author": "Vanson Leung",
  "type": "module",
  "scripts": {
    "prepare:runtime": "node scripts/build.mjs",
    "build": "npm run prepare:runtime && npm run typecheck && electrobun build",
    "start": "npm run build && node scripts/run.mjs",
    "start:built": "node scripts/run.mjs",
    "package": "npm run prepare:runtime && npm run typecheck && electrobun build --env=stable",
    "dist": "npm run package",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "electrobun prepare && tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "express": "^5.0.0",
    "sharp": "^0.35.4",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "electrobun": "2.0.1",
    "typescript": "~5.9.3"
  }
}
